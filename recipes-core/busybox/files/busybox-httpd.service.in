[Unit]
Description=Busybox HTTP daemon
After=network.target

[Service]
Type=forking
ExecStart=@sbindir@/httpd -h /srv/busybox-www -p 8080
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
